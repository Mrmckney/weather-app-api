{
    "version": 2,
    "builds": [
        {
            "src": "api.ts",
            "use": "@vercel/node"
        }
    ],
    "routes": [
        {
            "source": "/api/(.*)",
            "destination": "/api"
        }
    ]
}